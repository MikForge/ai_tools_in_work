# 测试报告审查清单

对每份测试报告按以下标准审查。发现问题归类为 严重 / 重要 / 轻微。

## 验证覆盖度
- [ ] 格式检查覆盖所有必要章节（YAML frontmatter、标题、代码块）
- [ ] 完整性检查覆盖所有引用文件和依赖
- [ ] 结构检查对照项目约定进行验证

## 结果清晰度
- [ ] 每个检查项的通过/失败状态明确
- [ ] 失败项包含 文件:行号 位置
- [ ] 失败项包含预期 vs 实际

## 反向检查（测试有效性）
- [ ] 如果所有检查通过但产物存在可见问题 → 标记为"验证边界不足"
- [ ] 验证项非平凡（不是仅"文件存在"这种）