<!-- 适用角色：所有角色 -->
<!-- 来源：agent 历史失败场景 -->

# 通知常量约束

每项约束都是硬规则。违反 = 节点执行失败 → 三类回写。

## 硬规则

| # | 约束 | 违规后果 | 来源 |
|---|------|----------|------|
| C10 | 通知常量统一在 `Commands.ts` 声明 | 禁止裸字符串 | agent 曾在多处硬编码字符串导致拼写不一致 |
| C11 | 常量命名空间隔离 | `Commands.UI.{ModuleName}.{Action}` | agent 曾把常量放在错误命名空间导致冲突 |

## 搜索模式
- 检测 C10 违规：grep 全项目查找 `sendNotification(` 调用中的裸字符串参数（非 `Commands.` 开头）
- 检测 C11 违规：检查 `Commands.ts` 中新增常量的命名空间是否遵循层级规范
