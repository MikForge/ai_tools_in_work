# Session 连接池优化方案

## 摘要

登录接口在并发超过 100 QPS 时，session 服务连接池（默认大小 10）耗尽导致直接返回 502 错误。本提案将连接池上调至 50，并新增缓存降级策略。

## 动机

大促活动期间并发登录请求超过 100 QPS 时，登录接口持续返回 502。根因：session 服务连接池默认仅 10 个连接，并发超出后无降级路径。

## 方案概述

1. 连接池从 10 扩容至 50
2. 连接池耗尽时回退到缓存 session，而非直接 502
